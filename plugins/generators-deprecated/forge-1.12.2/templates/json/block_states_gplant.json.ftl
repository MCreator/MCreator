{
    "variants": {
      "age=0": {
        "model": "${modid}:${registryname}"
      },
      "age=1": {
        "model": "${modid}:${registryname}"
      },
      "age=2": {
        "model": "${modid}:${registryname}"
      },
      "age=3": {
        "model": "${modid}:${registryname}"
      },
      "age=4": {
        "model": "${modid}:${registryname}"
      },
      "age=5": {
        "model": "${modid}:${registryname}"
      },
      "age=6": {
        "model": "${modid}:${registryname}"
      },
      "age=7": {
        "model": "${modid}:${registryname}"
      },
      "age=8": {
        "model": "${modid}:${registryname}"
      },
      "age=9": {
        "model": "${modid}:${registryname}"
      },
      "age=10": {
        "model": "${modid}:${registryname}"
      },
      "age=11": {
        "model": "${modid}:${registryname}"
      },
      "age=12": {
        "model": "${modid}:${registryname}"
      },
      "age=13": {
        "model": "${modid}:${registryname}"
      },
      "age=14": {
        "model": "${modid}:${registryname}"
      },
      "age=15": {
        "model": "${modid}:${registryname}"
      }
    }
}