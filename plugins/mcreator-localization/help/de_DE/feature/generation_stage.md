Diese Option legt fest, in welchem Stadium der Weltgenerierung das Feature hinzugefügt werden soll.

Die Stufen werden in der Reihenfolge sortiert, in der sie erzeugt werden, wobei "RAW_GENERATION" zuerst erzeugt wird und "TOP_LAYER_MODIFICATION" zuletzt erzeugt wird.