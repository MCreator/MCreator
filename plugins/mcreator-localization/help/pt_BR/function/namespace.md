* **Mod:** É usado apenas para seu mod. (Chame uma função como `${modid}:${registryname}`)

* **Minecraft:** É usado com algumas opções do Minecraft (chame essa função como `minecraft:${registryname}`)