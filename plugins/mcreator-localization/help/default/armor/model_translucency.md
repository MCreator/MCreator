This parameter controls if the selected custom java model supports translucency in its rendering.
Turn this on only for models intended to display translucent textures, as translucent rendering can 
result in reduced performance when used unnecessarily.