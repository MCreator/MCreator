Specify a procedure to generate features from

This procedure will run on top of teh existing configured_feature

Specifying "empty feature" for the feature configuration is recommended; so that only the procedure runs