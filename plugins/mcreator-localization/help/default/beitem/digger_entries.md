The following list declares all possible blocks and tags that will be affected by this tool.
Each entry can define the speed at which this tool will break the selected block/blocks from the tag.

If a block (as a standalone block or part of a tag) is not present inside this list, the block will keep the normal behavior.