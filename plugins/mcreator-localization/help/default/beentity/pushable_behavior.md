These parameters define how this entity will react to collisions with other entities.

`${l10n.t("elementgui.beentity.is_pushable_by_entities")}` defines whether other entities can push this entity.