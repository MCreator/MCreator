These parameters define how this entity will react to collisions with other entities and pistons.

`${l10n.t("elementgui.beentity.is_pushable")}` defines whether other entities can push this entity.
`${l10n.t("elementgui.beentity.is_pushable_by_piston")}` defines whether pistons can safely push this entity.